<Page x:Class="_3ISIP223_PogosyanWPF.Pages._6AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_PogosyanWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" d:Background="White"
      Title="_6AdminPage">

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <TextBlock Text="Пользователи (8)" Margin="10"  FontSize="15" />
        <Button Grid.Column="3" Grid.Row="0" Content="Добавить" HorizontalAlignment="Right" Style="{StaticResource ConfirmButton}"/>

        <ListBox d:ItemsSource="{d:SampleData ItemCount=4}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" BorderBrush="{x:Null}" Grid.Row="1" Grid.ColumnSpan="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="ИВАНОВА АННА СЕРГЕЕВНА'" Grid.Row="0" Margin="0 0 10 0"  FontSize="15" />


                        <Border Padding="5" Grid.Row="1" BorderThickness="1" Margin="0 5"  BorderBrush="Black" >
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0 0 0 15">
                                    <TextBlock Text="Тел: +7 (999) 123-45-67" FontSize="15" />
                                    <TextBlock Text=" │ "  FontSize="15" />
                                    <TextBlock Text="Почта: anna@example.com"  FontSize="15" />
                                </StackPanel>
                                <Grid Margin="0 0 0 15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="25"/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Роль: " Grid.Column="0"  FontSize="15" />
                                    <TextBlock Text="Мастер" Grid.Column="1"  FontSize="15" />

                                    <TextBlock Text="Статус: "  Grid.Column="0" Grid.Row="1"  FontSize="15" />
                                    <TextBlock Text="АКТИВЕН" Grid.Column="1" Grid.Row="1" FontSize="15" />

                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" Content="РЕДАКТИРОВАТЬ" Margin="10 0" />
                                    <Button Grid.Column="1" Content="ЗАМОРОЗИТЬ"  Margin="10 0" Style="{StaticResource FreezeButton}"/>
                                    <Button Grid.Column="3" Content="УДАЛИТЬ"   Margin="10 0" Style="{StaticResource DeleteButton}"/>

                                </Grid>

                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>

</Page>
