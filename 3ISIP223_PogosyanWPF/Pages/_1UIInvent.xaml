<Page x:Class="_3ISIP223_PogosyanWPF.Pages._1UIInvent"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_PogosyanWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="50" d:DesignWidth="133"
      Title="_1UIInvent">

    <Page.Resources>
        <Style TargetType="Border" x:Key="ElementInvent">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF836440"/>
                    <Setter Property="BorderBrush" Value="#FF656363"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid VerticalAlignment="Bottom" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        
        <Image Grid.ColumnSpan="3" Source="/Icons/Invent.png" />


        <Border Grid.Column="0" Width="35"  Margin="5,7,0,0" HorizontalAlignment="Right" Height="34"  Style="{StaticResource ElementInvent}">
            <Image  Source="{Binding Player.Weapon.pathImg}" RenderTransformOrigin="0.5,0.5" >
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-45"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
        </Border>

        <Border Grid.Column="1" Width="35"  Margin="5,7,0,0" HorizontalAlignment="Left" Height="34"  Style="{StaticResource ElementInvent}">

            <Image Source="{Binding Player.Armor.pathImg}"/>
        </Border>
        
        <Border Grid.Column="2" Width="34"  Margin="0,7,0,0" HorizontalAlignment="Left" Height="31"  Style="{StaticResource ElementInvent}">


            <Image Source="/Images/potions/potionHP.png" />
        </Border>


        <!--<Button  Grid.Column="2" Width="35"  Margin="0,7,0,0" HorizontalAlignment="Left" Background="Transparent">
            <Image Source="{Binding Player.Armor.pathImg}"/>
        </Button>-->
        
        
    </Grid>
</Page>
