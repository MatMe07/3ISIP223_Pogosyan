<Window x:Class="_3ISIP223_PogosyanWPF.Winodws.AddEditProductTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Тип товара" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="Добавление типа товара" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Название типа товара:"/>
        <TextBox x:Name="txtName" Grid.Row="2" Margin="0,5,0,20"/>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Отмена" Grid.Column="0" Margin="5" 
                    Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
            <Button Content="Сохранить" Grid.Column="1" Margin="5" 
                    Style="{StaticResource ConfirmButton}" Click="Save_Click"/>
        </Grid>
    </Grid>
</Window>