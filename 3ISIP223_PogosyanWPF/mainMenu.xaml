<Window x:Class="_3ISIP223_PogosyanWPF.mainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3ISIP223_PogosyanWPF"
        mc:Ignorable="d"
        Title="mainMenu" Height="229" Width="200" Background="Transparent" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Window.Resources>
        <Style TargetType="Border" x:Key="ElementInvent">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <!--<Setter Property="Background" Value="#FF836440"/>-->
                    <!--<Setter Property="BorderBrush" Value="#FF656363"/>-->
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Grid Height="229" Width="200" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="/Icons/interface/frameCancel.png" Stretch="Uniform"/>
        </Grid.Background>
        <Border Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Width="23"  Margin="0,5,7,0" Style="{StaticResource ElementInvent}" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Image Source="/Icons/interface/CencelButton.png"  />
            
        </Border>

        <TextBlock Grid.Row="1" Text="Dungeon" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <Grid Grid.Row="2" Margin="0 15">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border Style="{StaticResource ElementInvent}" Grid.Row="0" Padding="13 5" MouseLeftButtonDown="BorderBtnStart_MouseLeftButtonDown" >
                <Image Source="/Icons/interface/btnStart.png"/>
            </Border>
            <Border Style="{StaticResource ElementInvent}" Grid.Row="1" Padding="13 5" MouseLeftButtonDown="Border_MouseLeftButtonDown" >
                <Image Source="/Icons/interface/btnQuit.png"/>
            </Border>
        </Grid>
        
    </Grid>
</Window>
